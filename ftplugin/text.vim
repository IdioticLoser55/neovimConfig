" wraps lines so they don't go offscreen. local means only in the current
" window.
setlocal wrap
" makes it so that when wrapping lines words aren't broken in half.
setlocal linebreak

set nonumber norelativenumber
